# Replace `./app.db` with the path to where you want the project's database to reside
DATABASE_URL="file:./app.db"
# Where your project will run (e.g., http://myserver.local:8433)
ORIGIN=""

# Better Auth
# For production use 32 characters and generated with high entropy
# https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET=""

# Registration token to allow signups.
# Keep in mind, any registered user can manage ALL the drives, so this should be kept secret and only given to trusted users.
REGISTRATION_TOKEN="REPLACEME"

# Optional: prepend this prefix to all paths before passing them to Restic or the filesystem.
# Set automatically by compose.yaml when using Docker (PUBLIC_HOST_PREFIX=/host).
# Leave unset for bare-metal deployments.
# PUBLIC_HOST_PREFIX=""


# Set port for the server to listen on
PORT=8433